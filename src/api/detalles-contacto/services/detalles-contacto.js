'use strict';

/**
 * detalles-contacto service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::detalles-contacto.detalles-contacto');
